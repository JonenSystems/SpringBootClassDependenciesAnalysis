request_id: REQ-007.011
level: lower
title: SecurityContextの解析
## demand:
  ①何が（何を）: `SecurityContextHolder`参照依存関係を
  ②どんな状態／条件で: `.java`ソースコードのAPI呼び出しを解析する際に
  ③どうする: 抽出・記録する
  文: `SecurityContextHolder`参照依存関係を、`.java`ソースコードのAPI呼び出しを解析する際に抽出・記録する
## reason:
  - SecurityContextの依存関係を可視化することで、セキュリティコンテキストの利用箇所を把握できる
## description:
  - 用語: SecurityContextとは、`SecurityContextHolder`参照依存を指す
  - 範囲: JavaParserを使用して`SecurityContextHolder.getContext()`呼び出し箇所を記録する
  - 例: `SecurityContextHolder.getContext().getAuthentication()`の場合、呼び出し元→SecurityContextの依存関係が存在する


## specification_note: 
  - 解析タイプ:  静的解析         
  - 検索対象:  `.java`（API呼び出し）                       
  - 解析ツール:  JavaParser                      
  - 解析内容:  `SecurityContextHolder.getContext()`呼び出し箇所を記録                              


